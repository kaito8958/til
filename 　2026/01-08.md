## TIL: new_record? と三項演算子の使い分け

- `new_record?` は Rails（ActiveRecord）が用意しているメソッドで、「まだDBに保存されていないか？」を true / false で返す
- すでに保存されている場合は `persisted?` が true になる
- Rubyでは `?` がつくメソッドは「状態を質問する」意味を持つ

### 例
```ruby
post.new_record? ? "投稿する" : "更新する"
