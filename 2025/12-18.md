# TIL（2025-12-18）

- 現代ブラウザでは beforeunload のカスタム文言は無視されるため、preventDefault() と returnValue = '' を使うのが互換的な書き方であることを学んだ

- ステージング環境でのみ修正した内容は、PR対象のブランチにマージしないとレビュー対象に含まれない点に注意が必要

- 挙動確認はスクリーンショットだけでなく、短い動画（Gyazo Video）で共有するとレビューしやすい

- エラーログや長い出力は Slack では本文に直接貼らず、テキストスニペットとして共有するのが実務的である
